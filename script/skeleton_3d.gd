extends Skeleton3D


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass
	#var physical_bones_mod = $Skeleton3D.modification_stack[0] as SkeletonModification3DPhysicalBones
	#physical_bones_mod.blend_factor = 0.0


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
